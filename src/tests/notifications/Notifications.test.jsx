import { describe, expect, test } from "vitest";

describe("Notifications component", () => {
  test("Renderizar card de noptificacion", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad del boton de aceptar", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad del boton de rechazar", () => {
    expect(1 + 1).toBe(2);
  });
});
