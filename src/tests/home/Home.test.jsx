import { describe, expect, test } from "vitest";

describe("Home component", () => {
  test("Renderizar popover de cierre de sesion", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad de boton de cierre de sesion", () => {
    expect(1 + 1).toBe(2);
  });
  test("Listado de proyectos de los usuarios", () => {
    expect(1 + 1).toBe(2);
  });
  test("Listado de cards en los proyectos que colabora", () => {
    expect(1 + 1).toBe(2);
  });
  test("Renderizado de la barra de busqueda", () => {
    expect(1 + 1).toBe(2);
  });
});
