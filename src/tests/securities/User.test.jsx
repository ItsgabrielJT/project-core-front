import { describe, expect, test } from "vitest";

describe("User component", () => {
  test("Renderizar pantalla de detalle de perfil", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad del boton de edicion", () => {
    expect(1 + 1).toBe(2);
  });
  test("Renderizado del componente de estadisticas", () => {
    expect(1 + 1).toBe(2);
  });
});
