import { describe, expect, test } from "vitest";

describe("Project create or edit component", () => {
  test("Renderizar el formulario del proyecto", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad del boton de guardar", () => {
    expect(1 + 1).toBe(2);
  });
  test("Renderizar informacion del componente", () => {
    expect(1 + 1).toBe(2);
  });
});
