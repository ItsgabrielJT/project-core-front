import { describe, expect, test } from "vitest";

describe("Colaborators component", () => {
  test("Renderizar el modal con colaboradores", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcioanlidad de los checks de permisos", () => {
    expect(1 + 1).toBe(2);
  });
  test("Rendirizar popover de colaboradores", () => {
    expect(1 + 1).toBe(2);
  });
  
});
