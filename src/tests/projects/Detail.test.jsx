import { describe, expect, test } from "vitest";

describe("Project detail component", () => {
  test("Renderizar el detalle del proyecto", () => {
    expect(1 + 1).toBe(2);
  });
  test("Renderizar el detalle del creador del proyecto", () => {
    expect(1 + 1).toBe(2);
  });
  
});
