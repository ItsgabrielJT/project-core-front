import { describe, expect, test } from "vitest";

describe("Projects component", () => {
  test("Renderizar card de proyecto", () => {
    expect(1 + 1).toBe(2);
  });
  test("Renderizar modal de confirmacion", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad boton de confirmacion", () => {
    expect(1 + 1).toBe(2);
  });
  
});
