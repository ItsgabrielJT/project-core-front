import { describe, expect, test } from "vitest";

describe("Admin Projects component", () => {
  test("Renderizar card de proyectos", () => {
    expect(1 + 1).toBe(2);
  });
  test("Renderizar modal de confirmacion de eliminacion", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad boton de eliminacion", () => {
    expect(1 + 1).toBe(2);
  });
  
});
