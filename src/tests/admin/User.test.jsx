import { describe, expect, test } from "vitest";

describe("Admin users component", () => {
  test("Renderizar pantalla de detalle de perfil", () => {
    expect(1 + 1).toBe(2);
  });
  test("Renderizar pantalla de listado de usuarios", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad del boton de edicion", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad del boton de eliminacion de usuario", () => {
    expect(1 + 1).toBe(2);
  });
});
