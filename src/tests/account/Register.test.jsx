import { describe, expect, test } from "vitest";

describe("Register component", () => {
  test("Renderizar modal 1", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad del boton de siguiente", () => {
    expect(1 + 1).toBe(2);
  });
  test("Renderizar modal 2", () => {
    expect(1 + 1).toBe(2);
  });
});
