import { describe, expect, test } from "vitest";

describe("Login component", () => {
  test("renderizar formulario", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcionalidad el boton de inicio de sesion", () => {
    expect(1 + 1).toBe(2);
  });
});
