import { describe, expect, test } from "vitest";

describe("Reset password component", () => {
  test("Renderizar modal de email", () => {
    expect(1 + 1).toBe(2);
  });
  test("Funcaionalidad de boton", () => {
    expect(1 + 1).toBe(2);
  });
});
